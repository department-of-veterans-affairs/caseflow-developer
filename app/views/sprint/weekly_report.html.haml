- unless @weekly_report.nil?
  %h2 Weekly Report

.cf-table-wrap
  %table.usa-table-borderless.cf-table-borderless
    %thead
      %tr
        %th{"data-field" => "id"} #
        %th{"data-field" => "id"} Created On
        %th{"data-field" => "description"} Description
        %th{"data-field" => "labels"} Product
        %th{"data-field" => "labels"} Labels
        %th{"data-field" => "assignees"} Assignees
    %tbody
   
  - @weekly_report.group_by {|issue| }.each do |group, issues|
    %h2= group.to_s 
   
    
    %div{:class => "list-#{group}"}    
    - issues.each do |issue| 
      %div= issue[:number]  
      %div= issue[:created_at] 
      %div= issue[:title]
      %div= issue[:labels][2][:name]
      %div= issue[:assignees].map{|assignee| assignee[:login]}.join(",")
    
       
  